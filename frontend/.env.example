# 后端 API 地址（开发环境默认连本机 10086 端口）
VITE_API_BASE_URL=http://localhost:10086

# WebSocket 实时比分（可选，默认同主机 10086）
VITE_WS_URL=ws://localhost:10086/ws/live

# 站点 canonical URL（构建时写入 favicon / 微信分享 og:image，生产勿带末尾斜杠）
# 例：https://loveaibaby.cn
VITE_SITE_URL=https://loveaibaby.cn
